[task]

create = """
INSERT INTO tasks (title, description, status, priority, estimated_hours, assigned_to)
VALUES ($1, $2, $3, $4, $5, $6)
RETURNING *;
"""

get_all = "SELECT * FROM tasks;"

get_by_id = "SELECT * FROM tasks WHERE id=$1;"