[task]

create = """
INSERT INTO tasks (title, description, status, priority, estimated_hours, assigned_to, project_name, start_date, end_date)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)
RETURNING *;
"""

get_all = "SELECT * FROM tasks;"

get_by_id = "SELECT * FROM tasks WHERE id=$1;"